<% # expects admin_banner %>
<% # don't forget to update layouts/banner! %>
<div class="<%= admin_banner.banner_type != '' ? admin_banner.banner_type : 'announcement' %> banner">
  <%= raw sanitize_field(admin_banner, :content, image_safety_mode: true) %>
</div>
