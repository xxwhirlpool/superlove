<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="x-ua-compatible" content="ie=edge" />
    <meta name="keywords" content="fanfiction, fanworks, creative works" />
    <meta name="language" content="en-US" />
    <meta name="subject" content="fandom" />
    <meta name="description" content="superlove, what me and your mother make when you're not home" />
    <meta name="distribution" content="GLOBAL" />
    <meta name="classification" content="slay" />
    <meta name="author" content="girl on the moon" />
    <%= csrf_meta_tags %>
    <% item = @user || @work || @series %>
    <% if disallow_robots?(item) %>
      <meta name="robots" content="noindex" />
      <meta name="googlebot" content="noindex" />
    <% end %>
  	<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  	  <%= display_meta_tags(default_meta_tags) %>
    <meta name="chrome" content="nointentdetection" />
    <meta name="format-detection" content="telephone=no" />
    <title><%= browser_page_title(@page_title, @page_subtitle) %></title>
    <%= render :partial => 'layouts/includes' %>
    <%= phraseapp_in_context_editor_js %>
  </head>

  <body class="<%= logged_in? || logged_in_as_admin? ? 'logged-in' : 'logged-out' %><%= logged_in_as_admin? ? ' admin' : '' %>" <%= PhraseApp::InContextEditor.enabled? ? "data-no-turbolink" : "" %>>
    <!--
      <%= controller.controller_name %>
    -->
    <div id="outer">
      <ul id="skiplinks"><li><a href="#main"><%= ts('Main Content') %></a></li></ul>
      <% # layouts/proxy_notice is rendered here by JavaScript found in layouts/javascripts. %>
      <%= render :partial => 'layouts/header' %>
      <div id="inner">
        <!-- BEGIN main -->
        <main id="main" class="<%= classes_for_main %>" role="main">
          <div id="flash-container">
            <%= flash_div :error, :caution, :notice, :alert %>
            <%= yield :flash %>
          </div>
          <!-- BEGIN sidebar -->
          <%= yield :sidebar %>
          <!-- END sidebar -->
          <div id="content">
            <%= yield :breadcrumbs %>
            <%= yield %>
          </div>
        </main>
        <!-- END main -->
      </div>
      <%= render :partial => 'layouts/footer' %>
    </div>
    <%= render :partial => 'layouts/javascripts' %>
  </body>
</html>
